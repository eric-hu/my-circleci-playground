machine:
  services:
    - docker
  environment:
dependencies:
  pre:
    # nothing for now
  override:
test:
  override:
    # nothing for now
deployment:
  dev:
    branch: master
    commands:
      - docker build -t daluu/my-circleci-playground .
